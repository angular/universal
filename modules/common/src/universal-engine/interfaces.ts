import { StaticProvider } from '@angular/core';
/**
 * These are the allowed options for the render
 */
export interface ɵRenderOptions {
  providers?: StaticProvider[];
  request: any;
  response?: any;
}
